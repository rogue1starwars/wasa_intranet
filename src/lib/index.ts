export type CardData = {
  title: string;
  description: string;
  map: string;
};

export type BoxData = {
  title: string;
  shelf: number;
  box: string;
  x_center: number;
  y_center: number;
};
